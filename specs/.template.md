# {Application Name} - Specification

**Version:** 1.0.0
**Template:** Spernakit v1.7.0
**Stack:** SPERN (SQLite, Prisma, Express, React, Node.js on Bun)

## Overview

{Brief description of the application purpose and main goals}

## Technology Stack

### Spernakit Architecture

- **Core Stack**: SPERN (SQLite, Prisma, Express, React, Node.js)
- **Runtime**: Bun for both frontend and backend services
- **Package Manager**: Bun for dependency management and scripts
- **Monorepo Structure**: Workspace architecture with frontend/backend separation
- **Configuration**: JSON-driven config via `config/{appname}.json` (no implicit .env loading)

### Frontend

- **Framework**: React 19 with Vite 5
- **Styling**: Tailwind CSS with DaisyUI component library
- **State Management**: React hooks and context
- **Routing**: React Router with ProtectedRoute components for RBAC-aware routing
- **Data Fetching**: TanStack Query (React Query) for server state
- **Port**: {frontend_port} (default: 3330)

### Backend

- **Runtime**: Node.js with Express 5, running on Bun
- **Database**: SQLite with Prisma ORM (PostgreSQL-compatible schema)
- **Authentication**: JWT with HTTP-only cookies and 5-tier RBAC system
- **Port**: {backend_port} (default: 3331)

### Configuration

All configuration stored in `config/{appname}.json`:

- Server ports and URLs
- Database connection strings
- Security keys and secrets
- Feature flags and environment settings
- CORS and rate limiting settings

## Prerequisites

### Environment Setup

- Application configuration stored in `config/{appname}.json`
- Frontend dependencies installed via `bun install` in `/frontend`
- Backend dependencies installed via `bun install` in `/backend`
- Database bootstrapped via `bun run db:setup`
- Development assumes Bun environment with no implicit .env loading

### Development Commands

```bash
bun run init          # Initialize workspaces and install deps
bun run dev           # Start both frontend and backend
bun run dev:backend   # Start Express backend only
bun run dev:frontend  # Start React frontend only
bun run build         # Build both frontend and backend
bun run format        # Format code with Prettier
bun run lint          # Lint code with ESLint
bun run db:setup      # Generate Prisma client, run migrations, seed data
```

## Core Features

{List main features with bullet points}

## Database Schema

### Tables

{Describe database tables and relationships}

## API Endpoints Summary

{List main API endpoints by category}

## UI Layout

### Main Structure

{Describe the overall UI layout}

### Components

{List key UI components}

## Implementation Steps

### Step 1: Project Foundation

{Tasks for initial setup}

### Step 2: Authentication & Security

{Tasks for auth and RBAC}

### Step 3: Core Features

{Tasks for main functionality}

### Step 4: Polish & Testing

{Tasks for finalization}

## Success Criteria

### Functionality

{Criteria for functional requirements}

### User Experience

{Criteria for UX requirements}

### Technical Quality

{Criteria for technical requirements}

### Security & Compliance

{Criteria for security requirements}

## Spernakit-Specific Optimizations

### Performance

- Virtual scrolling for large lists
- Strategic DB indexing
- Code splitting and lazy loading
- Bundle optimization

### Security

- 5-tier RBAC with inheritance
- Comprehensive audit trail
- Input validation with shared schemas
- CSP, CSRF protection, secure cookies
- Rate limiting with per-IP rules

### Development Workflow

- Single set of Bun scripts for lint/format/build/test
- Shared TypeScript types for API contracts
- Standardized error response format
- Automated migrations
- JSON-based configuration

## Integration Dependencies

### Third-party Libraries

{List required libraries}

### API Integrations

{List external API integrations}

## Deployment Strategy

- Monolithic Docker container with nginx + supervisord
- Frontend port (3330) exposed externally
- Health and metrics endpoints for orchestration
- Persistent volume mounts for database and config
- Environment-specific config via JSON files
